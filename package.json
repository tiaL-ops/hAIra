{
  "name": "haira",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "haira-client",
    "haira-server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c magenta.bold,cyan.bold \"npm --prefix haira-server run dev\" \"npm --prefix haira-client run dev\"",
    "dev:server": "npm --prefix haira-server run dev",
    "dev:client": "npm --prefix haira-client run dev"
  },
  "devDependencies": {
    "chai": "^6.2.0",
    "concurrently": "^9.1.2",
    "mocha": "^11.7.4"
  },
  "dependencies": {
    "@google/genai": "^1.25.0",
    "@tiptap/extension-text-align": "^3.7.2",
    "lucide-react": "^0.546.0"
  }
}
